name: Test SDK
on:
  [push]
  # push:
  #   paths:
  #     - "packages/cli/**"
  #   branches:
  #     - main
jobs:
  deploy-vercel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: |
          cd packages/sdk
          yarn
          yarn tsc --noEmit
